// ─────────────────────────────────────────────────────────────────────────
// Company branding configuration
//
// Every value shown on the generated property post (and in the app footer,
// where noted) is read from this single file. Update the details here and
// they will automatically appear everywhere in the app — no need to hunt
// through components.
// ─────────────────────────────────────────────────────────────────────────

export const branding = {
  // Full company name, shown in the post footer.
  companyName: "R&J. Housing LLP",

  // Contact line shown in the post footer. Replace the placeholder digits
  // with a real phone number (or email / WhatsApp link text) before sharing.
  contact: "+91 XXXXX XXXXX",

  // Short label shown before the contact line, e.g. "Contact:" or "Call:".
  contactLabel: "Contact",

  // 2–4 letter monogram used in the brand mark badge on the post.
  monogram: "R&J.",

  // Optional short line shown under the company name on the post footer.
  // Leave as an empty string to omit it.
  tagline: "Real Estate & Housing Partners",

  // Optional website / handle line. Leave as an empty string to omit it.
  website: "https://www.r&j.com",
};

// App-level (not shown on the property post itself) attribution.
export const appMeta = {
  builtBy: "Radhika Jejurkar",
};
